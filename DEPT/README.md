# SQL Dork Scanner (DEPT)

## 📝 Описание

Специализированный сканер для поиска SQL уязвимостей с использованием Google/Bing дорков. Автоматизирует процесс поиска потенциально уязвимых веб-приложений.

## 🎯 Назначение

- Автоматический поиск SQL уязвимостей
- Использование специализированных SQL дорков
- Фильтрация результатов по SQL-специфичным словам
- Пентестинг и аудит безопасности

## ⚠️ КРИТИЧЕСКИ ВАЖНО

**ТОЛЬКО ДЛЯ ЛЕГАЛЬНОГО ТЕСТИРОВАНИЯ!**

- ✅ Тестирование собственных систем
- ✅ Пентест с письменным разрешением
- ✅ Образовательные цели
- ❌ НЕ для взлома
- ❌ НЕ для несанкционированного доступа
- ❌ Нарушение закона = уголовная ответственность

**Незаконное использование этого инструмента может привести к тюремному заключению!**

## 📋 Файлы и структура

### Основной скрипт
- **`script.py`** - Главный скрипт сканера

### Входные данные
- **`dork.txt`** - Список SQL дорков

### Подпапки

#### `bing_parser/`
Встроенный парсер поисковой системы:
- `parse.py` - Парсер результатов
- `output.txt` - Найденные URL
- `temp` - Временные файлы

#### `settings/`
Конфигурационные файлы:
- **`sql.ini`** - Настройки SQL проверок
- **`sql_words.ini`** - SQL-специфичные слова для фильтрации
  ```
  mysql_fetch_array
  SQL syntax error
  mysql_num_rows
  You have an error in your SQL
  Warning: mysql
  ```

#### `output/`
Результаты сканирования

## 🔧 Требования

```bash
pip install requests
pip install beautifulsoup4
```

## 🚀 Использование

### 1. Подготовка дорков

Создайте `dork.txt` с SQL-специфичными дорками:
```
inurl:".php?id=" intext:"mysql_fetch_array()"
inurl:".php?id=" intext:"You have an error in your SQL syntax"
inurl:"page.php?id=" intext:"Warning: mysql"
inurl:"news.php?id=" intext:"SQL syntax"
site:*.edu inurl:"product.php?id="
```

### 2. Настройка фильтров

Отредактируйте `settings/sql_words.ini`:
```ini
mysql_fetch_array
mysql_num_rows
mysqli_query
SQL syntax error
mysql_error
You have an error in your SQL syntax
```

### 3. Запуск

```bash
python script.py
```

## 📊 Как работает

### 1. Загрузка дорков
Читает SQL дорки из `dork.txt`

### 2. Поиск через парсер
Использует встроенный `bing_parser/` для поиска

### 3. Фильтрация результатов
Проверяет найденные страницы на наличие SQL-специфичных слов из `sql_words.ini`

### 4. Сохранение
Сохраняет потенциально уязвимые URL в `output/`

## 🔍 Примеры SQL Дорков

### Классические SQL Injection дорки
```
inurl:".php?id=" intext:"mysql_"
inurl:".php?catid=" intext:"Warning: mysql"
inurl:"viewarticle.php?id=" 
inurl:"newsitem.php?num="
```

### Специфичные для CMS
```
# WordPress
inurl:"wp-content/plugins" intext:"SQL"

# Joomla
inurl:"index.php?option=com_" intext:"mysql"

# PHP-Nuke
inurl:"modules.php?name=" intext:"SQL"
```

### Информационные страницы
```
intext:"SQL syntax error" site:edu
intext:"mysql_fetch_array()" site:gov
intext:"Warning: mysql_" filetype:php
```

## 📈 Процесс сканирования

```
1. Загрузка дорков из dork.txt
   └─> 50 дорков загружено

2. Поиск через поисковую систему
   ├─> Дорк 1: 15 результатов
   ├─> Дорк 2: 23 результата
   └─> Дорк 3: 8 результатов

3. Проверка на SQL маркеры
   ├─> URL 1: mysql_fetch_array found ✓
   ├─> URL 2: No markers ✗
   └─> URL 3: SQL syntax error found ✓

4. Сохранение результатов
   └─> 2 потенциально уязвимых URL
```

## 🔍 Типы SQL уязвимостей

### Error-based SQL Injection
Ошибки MySQL в выводе:
```
mysql_fetch_array()
mysql_num_rows()
You have an error in your SQL syntax
```

### Blind SQL Injection
Проверка через поведение:
```
page.php?id=1' AND '1'='1
page.php?id=1' AND '1'='2
```

### Time-based SQL Injection
```
page.php?id=1' AND SLEEP(5)--
```

## 💡 Настройка sql_words.ini

### Базовые маркеры
```ini
[MYSQL_ERRORS]
mysql_fetch_array
mysql_num_rows
mysql_query
mysqli_

[SQL_ERRORS]
SQL syntax
syntax error
unclosed quotation mark

[WARNINGS]
Warning: mysql
Warning: mysqli
```

## 🐛 Решение проблем

### Проблема: Много ложных срабатываний
**Решение:** 
- Уточните дорки
- Добавьте более специфичные слова в sql_words.ini
- Используйте дополнительную ручную проверку

### Проблема: Блокировка поисковиком
**Решение:**
- Добавьте задержки между запросами
- Используйте прокси
- Уменьшите количество дорков

### Проблема: Нет результатов
**Решение:**
- Проверьте работу bing_parser/
- Обновите дорки
- Попробуйте другие поисковики

## 💡 Улучшения

### 1. Автоматическая проверка уязвимости
```python
def test_sql_injection(url):
    payloads = ["'", "' OR '1'='1", "' AND '1'='2"]
    for payload in payloads:
        test_url = url + payload
        response = requests.get(test_url)
        if check_for_sql_error(response.text):
            return True
    return False
```

### 2. Отчеты
```python
def generate_report(vulnerabilities):
    with open('report.html', 'w') as f:
        f.write('<html><body>')
        f.write(f'<h1>Найдено уязвимостей: {len(vulnerabilities)}</h1>')
        for vuln in vulnerabilities:
            f.write(f'<p>{vuln}</p>')
        f.write('</body></html>')
```

### 3. Уровни серьезности
```python
severity = {
    'high': ['error in your SQL syntax'],
    'medium': ['mysql_fetch_array'],
    'low': ['mysql_num_rows']
}
```

## 📊 Ответственное раскрытие уязвимостей

Если вы нашли уязвимость:

1. **НЕ эксплуатируйте её!**
2. Свяжитесь с администратором сайта
3. Предоставьте детали (без публикации)
4. Дайте время на исправление (90 дней стандарт)
5. Получите разрешение перед публикацией

### Шаблон письма
```
Тема: Уязвимость безопасности на вашем сайте

Уважаемая команда безопасности,

Я обнаружил потенциальную SQL Injection уязвимость на вашем 
сайте example.com. Детали:

URL: http://example.com/page.php?id=1
Тип: SQL Injection
Серьезность: Высокая

Пожалуйста, свяжитесь со мной для получения подробностей.

С уважением,
[Ваше имя]
```

## 🔒 Безопасность

### Для владельцев сайтов

Защита от SQL Injection:
```php
// ❌ Уязвимый код
$query = "SELECT * FROM users WHERE id = " . $_GET['id'];

// ✅ Безопасный код
$stmt = $pdo->prepare("SELECT * FROM users WHERE id = ?");
$stmt->execute([$_GET['id']]);
```

### Рекомендации
1. Используйте Prepared Statements
2. Валидируйте ввод пользователя
3. Используйте ORM (Eloquent, Doctrine)
4. Применяйте WAF (Web Application Firewall)
5. Регулярные аудиты безопасности

## 📚 Дополнительные ресурсы

- [OWASP SQL Injection](https://owasp.org/www-community/attacks/SQL_Injection)
- [PortSwigger SQL Injection](https://portswigger.net/web-security/sql-injection)
- [SQL Injection Cheat Sheet](https://www.netsparker.com/blog/web-security/sql-injection-cheat-sheet/)

## 🔗 Связанные инструменты

- **search_engine_parser/** - Базовый парсер дорков
- Профессиональные инструменты: SQLMap, Burp Suite

## ⚖️ Правовая информация

Это инструмент для тестирования безопасности. Использование для:
- ✅ Пентестинга с разрешением
- ✅ Bug Bounty программ
- ✅ Образования
- ❌ Несанкционированного доступа - НЕЗАКОННО

**Знайте законы вашей страны!**

---

**Автор:** Jazis  
**Версия:** 1.0  
**Тип:** Security Testing Tool  
**Статус:** Только для легального использования  
**Последнее обновление:** 16 декабря 2025
