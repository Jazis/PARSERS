# AnonFiles Bruteforcer

## 📝 Описание

Инструмент для поиска файлов на платформе anonfiles.com методом перебора случайных идентификаторов. Генерирует случайные ID и проверяет их существование на сервере.

## 🎯 Назначение

- Брутфорс поиск файлов по случайным ID
- Обнаружение публично доступных файлов
- Логирование найденных файлов с их названиями
- Исследование файлового хостинга

## ⚠️ ВАЖНОЕ ПРЕДУПРЕЖДЕНИЕ

**Этот инструмент предназначен только для образовательных целей и исследования безопасности!**

- ❌ НЕ используйте для несанкционированного доступа к данным
- ❌ НЕ злоупотребляйте частотой запросов
- ✅ Уважайте ToS (Terms of Service) anonfiles.com
- ✅ Используйте ответственно и этично

## 📋 Файлы

- **`parser.py`** - Основной скрипт брутфорсера
- **`log.txt`** - Лог найденных файлов с названиями
- **`temp.txt`** - Временный файл для HTML ответов

## 🔧 Требования

```bash
pip install requests
```

Для Python 3:
```bash
pip3 install requests
```

## 🚀 Использование

### Запуск

```bash
python parser.py
```

### Интерактивный ввод

```
><
1000
```

Введите количество попыток перебора (например, 1000).

## 📊 Как работает

1. **Ввод количества попыток** - Пользователь указывает, сколько ID проверить
2. **Генерация ID** - Создает случайный 10-символьный ID
   ```python
   token = ''.join(random.choice(string.ascii_lowercase + string.digits + string.uppercase))
   ```
3. **Проверка существования** - Отправляет запрос на `https://anonfiles.com/{token}`
4. **Анализ ответа** - Проверяет наличие сообщения об ошибке
5. **Логирование** - Сохраняет информацию о найденных файлах

## 📈 Пример работы

```
><
1000
[1]Trying : ['aBcD123456']
Huita	1
[2]Trying : ['xYz9876543']
Huita	2
[3]Trying : ['qWeRty4321']
<h1 class="text-center text-wordwrap">important_file.zip</h1> qWeRty4321
```

## 📂 Выходные данные

### log.txt
Файл с найденными файлами:
```
<h1 class="text-center text-wordwrap">document.pdf</h1> aBcD123456
<h1 class="text-center text-wordwrap">archive.zip</h1> xYz9876543
```

### temp.txt
Временный файл с HTML последнего ответа (очищается после каждой проверки).

## 🔍 Алгоритм генерации ID

**Формат ID:** 10 символов  
**Символы:** a-z, A-Z, 0-9  
**Пример:** `aBcD123456`, `XyZ789qWer`

```python
token = ''.join(random.choice(
    string.ascii_lowercase +  # a-z
    string.digits +           # 0-9
    string.uppercase          # A-Z
) for x in range(10))
```

## 📊 Статистика

- **Счетчик попыток** - Отслеживает количество проверенных ID
- **Найденные файлы** - Логирует успешные совпадения
- **Процент успеха** - Очень низкий (случайный перебор)

## ⚡ Производительность

### Скорость
- Зависит от скорости интернет-соединения
- ~1-2 запроса в секунду (без оптимизации)
- Можно ускорить с многопоточностью

### Эффективность
- Вероятность найти файл крайне низкая
- ID пространство: 62^10 ≈ 839 квинтиллионов комбинаций
- Рекомендуется для демонстрации концепции, а не практического использования

## 💡 Улучшения

### 1. Многопоточность
```python
from concurrent.futures import ThreadPoolExecutor

with ThreadPoolExecutor(max_workers=10) as executor:
    executor.map(try_connect, tokens)
```

### 2. Прогресс-бар
```python
from tqdm import tqdm

for i in tqdm(range(kol0)):
    id_generator(token)
```

### 3. Сохранение прогресса
```python
# Сохранение проверенных ID
with open('checked_ids.txt', 'a') as f:
    f.write(token + '\n')
```

### 4. Rate limiting
```python
import time
time.sleep(0.5)  # Задержка между запросами
```

## 🐛 Решение проблем

### Проблема: "Connection Error"
**Решение:** Проверьте интернет-соединение

### Проблема: Блокировка по IP
**Решение:** Уменьшите частоту запросов, используйте прокси

### Проблема: Файл не скачивается
**Решение:** Скрипт только находит файлы, не скачивает их

### Проблема: Низкая эффективность
**Решение:** Это нормально для случайного перебора

## 🔄 Миграция на Python 3

```python
# Было (Python 2)
print str([schet.count0]) + 'Trying : ' + str([token])
kol0 = input()

# Стало (Python 3)
print(f"[{schet.count0}]Trying : [{token}]")
kol0 = int(input())
```

## 📝 Пример использования

### Тест на 100 ID
```bash
python parser.py
><
100
# Проверит 100 случайных ID
```

### Длительный поиск
```bash
python parser.py
><
10000
# Проверит 10,000 ID (займет несколько часов)
```

## 🔒 Этические соображения

### ✅ Допустимо:
- Исследование безопасности с разрешения
- Образовательные цели
- Тестирование собственных файлов

### ❌ Недопустимо:
- Поиск чужих приватных данных
- DDoS атаки на сервис
- Нарушение ToS платформы
- Распространение найденных данных

## 📚 Альтернативы

Если вам нужно найти конкретные файлы:
- Используйте официальный API (если доступен)
- Попросите прямую ссылку у владельца
- Используйте легальные методы поиска

## 🛡️ Безопасность

- Не запускайте с правами администратора
- Проверяйте логи на предмет чувствительных данных
- Используйте в изолированной среде
- Соблюдайте законодательство

---

**Автор:** Jazis  
**Версия:** 1.0  
**Python:** 2.7 (рекомендуется обновление до 3.x)  
**Статус:** Только для образовательных целей  
**Последнее обновление:** 16 декабря 2025
