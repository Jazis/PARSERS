# Domain Email Scraper

## 📝 Описание

Специализированный инструмент для извлечения email-адресов с веб-страниц по указанному домену. Ищет mailto: ссылки и извлекает email, относящиеся к конкретному домену.

## 🎯 Назначение

- Поиск email-адресов на веб-странице
- Фильтрация по домену (@example.com)
- Извлечение из mailto: ссылок
- Быстрый сбор контактов

## 📋 Файлы

- **`scr.py`** - Основной скрипт scraper
- **`Untitled-1.py`** - Альтернативная версия
- **`emails.txt`** - Выходной файл с найденными email
- **`temp`** - Временный файл для HTML
- **`www.txt`** - Список URL для обработки (опционально)

## 🔧 Требования

```bash
pip install requests
```

## 🚀 Использование

### Запуск

```bash
python scr.py
```

### Интерактивный ввод

```
Input domain:
@fa.ru

Input you link:
http://www.old.fa.ru/university/rectorate/Pages/rector.aspx
```

## 📊 Как работает

1. **Ввод домена** - Указываете домен для фильтрации (например, @fa.ru)
2. **Ввод URL** - Указываете страницу для парсинга
3. **Скачивание** - Получает HTML страницы
4. **Поиск mailto:** - Ищет ссылки с mailto:
5. **Фильтрация** - Извлекает только email указанного домена
6. **Сохранение** - Записывает в `emails.txt`

## 📈 Пример работы

### Запуск скрипта
```bash
python scr.py
Input domain:
@example.com
Input you link:
http://example.com/contacts
```

### Результат (emails.txt)
```
contact@example.com
info@example.com
support@example.com
```

## 🔍 Алгоритм извлечения

```python
# Ищет строки с доменом и mailto:
if domain and 'mailto:' in line:
    # Посимвольный поиск домена
    for i in range(len(line)):
        if domain in line[i]:
            # Извлекает email
```

## 💡 Особенности

### ✅ Преимущества
- Простой и быстрый
- Фильтрация по домену
- Минимальные зависимости

### ⚠️ Ограничения
- Ищет только на одной странице
- Только mailto: ссылки
- Может пропустить email в тексте
- Python 2 синтаксис

## 🐛 Решение проблем

### Проблема: Email не найдены
**Решение:** 
- Проверьте наличие mailto: на странице
- Убедитесь, что домен введен с @
- Попробуйте другую страницу сайта

### Проблема: Кодировка символов
**Решение:** 
```python
req0 = requests.get(link).text.encode('utf-8')
```

### Проблема: Неполные email
**Решение:** Проверьте алгоритм извлечения, возможно нужна доработка

## 💡 Улучшения

### 1. Регулярные выражения для email
```python
import re
email_pattern = r'\b[A-Za-z0-9._%+-]+@[A-Za-z0-9.-]+\.[A-Z|a-z]{2,}\b'
emails = re.findall(email_pattern, html)
emails = [e for e in emails if domain in e]
```

### 2. Поиск не только в mailto:
```python
# Поиск email в тексте страницы
all_emails = re.findall(email_pattern, html)
```

### 3. Обработка нескольких страниц
```python
urls = ['page1.html', 'page2.html', 'page3.html']
for url in urls:
    extract_emails(url, domain)
```

### 4. Валидация email
```python
from email_validator import validate_email
try:
    valid = validate_email(email)
except EmailNotValidError:
    pass
```

## 🔄 Миграция на Python 3

```python
# Было (Python 2)
print "Input domain:"
domain = raw_input()

# Стало (Python 3)
print("Input domain:")
domain = input()
```

## 📝 Примеры использования

### Сбор контактов университета
```bash
python scr.py
Input domain: @university.edu
Input you link: http://university.edu/contacts
```

### Сбор email отдела
```bash
python scr.py
Input domain: @company.com
Input you link: http://company.com/about/team
```

## 📂 Выходные данные

### emails.txt
Построчный список найденных email:
```
admin@example.com
info@example.com
contact@example.com
```

### temp
Временный HTML файл (можно удалить после работы).

## 🔒 Безопасность

### ✅ Рекомендации:
- Используйте только на публичных страницах
- Не собирайте email для спама
- Соблюдайте GDPR и другие законы о данных
- Проверяйте robots.txt

### ❌ Не используйте для:
- Массового спама
- Продажи email-баз
- Нарушения конфиденциальности

## 🌐 Расширенное использование

### Обработка списка URL
Создайте `www.txt`:
```
http://example.com/page1
http://example.com/page2
http://example.com/page3
```

Модифицируйте скрипт:
```python
with open('www.txt', 'r') as urls:
    for url in urls:
        process_url(url.strip(), domain)
```

## 📊 Статистика

Добавьте счетчик:
```python
count = 0
# После каждого найденного email
count += 1
print(f"Найдено email: {count}")
```

## 🔗 Связанные инструменты

- **email_extractor/** - Расширенный краулер с извлечением email
- **basic_web_crawler/** - Для сбора URL для обработки
- **silk_email_parser/** - Парсер для специфичных источников

---

**Автор:** Jazis  
**Версия:** 1.0  
**Python:** 2.7 (рекомендуется обновление до 3.x)  
**Последнее обновление:** 16 декабря 2025
